package com.summercoder.MVC.controller;

public abstract class ControllerTemplate implements IMenuControls
{
	/**
	 * all screen will have some resource in common(top menus, and other buttons)
	 * this will handle such common resouces
	 * we might need more familly of templates if we have many screen with unrelated(sort of) feature
	 */
	

}
